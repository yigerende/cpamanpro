# CPA Manager Plus v1.11.10

> 22 commits · 112 files changed · +30546 / -2253

> [English ->](https://github.com/seakee/CPA-Manager-Plus/blob/v1.11.10/docs/release-notes/v1.11.10-en.md)

## Overview

本次发布聚焦本地成本估算精度、认证文件操作安全性和实时监控可读性。模型价格同步会优先使用 models.dev 的第一方官方元数据，并把上下文阶梯与 Fast/Priority service tier 应用于 Dashboard、监控和账户历史；认证文件变更会按规范凭据身份校验，实时请求表则统一采用总延迟 TPS。

## Highlights

### Features

- 模型价格同步优先读取 models.dev catalog 的规范模型元数据，并依次使用 LiteLLM、OpenRouter 回退；只有唯一强身份匹配会自动保存，全部来源失败时会保留最后有效价格（`manager-server/model-pricing`）。
- 解析、校验并持久化 models.dev 上下文价格阶梯与显式 Fast/Priority 费率，通过持久化定价汇总将其应用到 Dashboard、监控、账户历史和前端用量成本（`manager-server/usage-pricing`、`web/usage`）。
- 模型价格页可只读展示同步的上下文阶梯和 service tier 规则，并按来源保留歧义候选；保存手动基础价格前会提示其将清除同步的高级规则（`web/model-prices`）。

### Fixes

- 认证文件的状态切换、删除、额度冷却恢复和巡检操作会按规范凭据身份校验，并在共享物理文件上串行协调变更；目标陈旧或身份歧义时会安全拒绝，避免作用到其他凭据（`manager-server/auth-files`、`web/auth-files`）。
- 实时请求表统一按总请求延迟计算单一 TPS，并改进紧凑列对齐、Token 明细、失败诊断和提示浮层可读性（`web/monitoring`）。

### Docs

- 中英文模型价格文档补充价格源优先级、同步故障回退、上下文阶梯、Fast/Priority 语义和手动覆盖边界（`docs/model-prices`）。

## Upgrade Notes

- Manager Server 启动时会自动迁移模型价格规则、定价汇总状态和凭据身份记录，无需手动执行数据库或配置迁移。
- 凭据身份所有权与冷却唯一键迁移后，旧版二进制不保证兼容；升级前请备份 SQLite 文件与 `data.key`，如需精确回退到 v1.11.9，请恢复升级前备份。
- Fast/Priority 与上下文阶梯估算依赖 CPA 用量事件中的 service tier、上下文和缓存字段；缺失时会使用兼容回退，Provider 账单仍是最终依据。
- 模型价格同步仍需用户手动触发；现有价格只会在同步、确认候选或手动编辑时改变，CPA 路由与上游计费行为不变。

---

**Full Changelog**: https://github.com/seakee/CPA-Manager-Plus/compare/v1.11.9...v1.11.10
